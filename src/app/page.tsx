import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import React from 'react'

function page() {
  return (
    <div>
      <Button
      variant={"new"}>Click me</Button>
    </div>
  )
}

export default page